<div class="items-section d-flex flex-column justify-content-between mt-5">
  <div class="container">
    <div class="align-items-start">
      <div class="col-8">
        <% @items.each do |item, count| %>
          <%= render 'item', item: item, count: count %>
        <% end %>
      </div>
      <div class="col-4">
        <%= render 'order' %>
      </div>
    </div>
  </div>
</div>