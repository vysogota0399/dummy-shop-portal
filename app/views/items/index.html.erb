<%= render 'filter', items: @items %>
<div class="items-section d-flex flex-wrap justify-content-between mt-5">
  <% @items.each do |item| %>
    <%= render 'item', item: item %>
  <% end %>
</div>
<%== pagy_bootstrap_nav(@pagy) %>